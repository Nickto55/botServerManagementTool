# Generic bot Dockerfile
FROM python:3.11-slim

WORKDIR /app

ENV PYTHONDONTWRITEBYTECODE=1 \
    PYTHONUNBUFFERED=1

COPY bots /app

RUN pip install --no-cache-dir -r requirements.txt || true

CMD ["python", "main.py"]
