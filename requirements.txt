Flask==3.1.0
Flask-SocketIO==5.4.1
eventlet==0.37.0
python-dotenv==1.0.1
bcrypt==4.2.1
Werkzeug==3.1.3
docker==7.1.0
GitPython==3.1.45
SQLAlchemy==2.0.35
gunicorn==23.0.0
flask-limiter==3.8.0
psutil==6.1.0
validators==0.34.0
parauseradd -m -s /bin/bash botops || true
mkdir -p /home/botops/.ssh
chown -R botops:botops /home/botops/.ssh
chmod 700 /home/botops/.ssh
# Сгенерировать ключ если не существует
sudo -u botops bash -c 'test -f /home/botops/.ssh/id_rsa || ssh-keygen -t rsa -b 4096 -N \"\" -f /home/botops/.ssh/id_rsa'
# Добавить в docker группу
usermod -aG docker botopsmiko==3.5.0
